# Project Agent Guidance

## Project-specific guidance

- Replace these placeholders with the repository's actual build, test, lint, and formatting commands.
- Describe the architecture and the smallest relevant validation command for each area.
- Record conventions an agent cannot reliably infer from the code.

## Working agreements

- Preserve existing repository conventions and user changes.
- Use `git` for repository operations Git supports; reserve `gh` for GitHub platform operations.
- Use test-driven development for behavioral changes and bug fixes unless it is unsuitable, in which case explain why first.
